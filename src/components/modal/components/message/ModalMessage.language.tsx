import {TranslateLanguageType} from 'locales/types';
import {ModalMessageLanguageType} from './ModalMessage.types';

const lang: TranslateLanguageType<ModalMessageLanguageType> = {
  ptBr: {
    success: 'Sucesso',
    error: 'Erro',
  },
  enUs: {
    success: 'Success',
    error: 'Error',
  },
};

export default lang;

import {GlobalLanguageType} from 'global/types/languages.type';
import {TranslateLanguageType} from 'locales/types';

const lang: TranslateLanguageType<GlobalLanguageType> = {
  ptBr: {
    change: 'Alterar',
    cpf: 'CPF',
  },
  enUs: {
    change: 'Change',
    cpf: 'CPF',
  },
};

export default lang;
